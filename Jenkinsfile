pipeline {
    agent any

    stages {

	    stage( 'Checkout' )  {
			steps {
				checkout scm;
			}
		}
		
		stage( 'Build Docker' )  {
			steps {
				checkout scm;
			}
		}
		
		stage( 'Deploy Docker' )  {
			steps {
				checkout scm;
			}
		}
    }
}
